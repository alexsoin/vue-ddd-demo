# Vue 3 DDD Architecture Template

Архитектурный шаблон для Vue 3 приложений с полным разделением бизнес-логики и UI, реализованный в стиле Domain-Driven Design (DDD).

## 🎯 Особенности

- **Domain-Driven Design**: Четкое разделение на Domain, Infrastructure и UI слои
- **TypeScript декораторы**: Декларативное описание правил трансформации и валидации
- **IndexedDB**: Локальное хранилище вместо реальных API (для демонстрации)
- **Множественные источники данных**: Поддержка объединения данных из нескольких источников с приоритетами
- **Composition API**: Vue 3 Composition API для реактивности
- **Tailwind CSS**: Современный UI с Tailwind CSS
- **Тестируемость**: Полная поддержка unit-тестов для бизнес-логики

## 📁 Структура проекта

```
src/
├─ domain/                    # Доменный слой (бизнес-логика)
│  ├─ entities/              # Domain entities (Employee, Department)
│  ├─ value-objects/        # Value Objects (DictionaryVO)
│  ├─ dto/                   # Data Transfer Objects
│  ├─ mappers/               # DTO ↔ Domain мапперы
│  ├─ repositories/         # Репозитории для работы с данными
│  └─ services/             # Бизнес-сервисы
├─ infrastructure/           # Инфраструктурный слой
│  ├─ indexeddb/           # IndexedDB клиент и адаптеры
│  ├─ decorators/           # TypeScript декораторы
│  └─ di/                   # Dependency Injection контейнер
├─ ui/                      # UI слой
│  ├─ components/          # Vue компоненты
│  ├─ composables/         # Vue composables
│  └─ pages/               # Страницы
├─ config/                  # Конфигурационные файлы
└─ test/                    # Тесты
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
# или
bun install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Запуск тестов

```bash
npm run test
```

### Запуск тестов с UI

```bash
npm run test:ui
```

### Сборка для продакшена

```bash
npm run build
```

## 📚 Архитектура

### Domain Layer (Доменный слой)

#### Entities
- **Employee**: Сущность сотрудника с методами `activate()`, `deactivate()`, `assignToDepartment()`
- **Department**: Сущность отдела

#### Value Objects
- **DictionaryVO**: Value Object для словарных значений `{code, name}`

#### Mappers
Мапперы преобразуют данные между DTO и Domain entities, используя метаданные декораторов:
- `EmployeeMapper`: Преобразование EmployeeDTO ↔ Employee
- `DepartmentMapper`: Преобразование DepartmentDTO ↔ Department

#### Repositories
Репозитории абстрагируют работу с данными:
- `EmployeeRepository`: Объединяет данные из нескольких источников (sourceA, sourceB)
- `DepartmentRepository`: Работа с отделами

#### Services
Сервисы содержат бизнес-логику:
- `EmployeeService`: Создание, обновление, деактивация сотрудников
- `DepartmentService`: Управление отделами

### Infrastructure Layer (Инфраструктурный слой)

#### IndexedDB
- `IDBClient`: Обёртка над IndexedDB с промисами
- `SourceAAdapter`, `SourceBAdapter`: Адаптеры для имитации внешних API
- `migrations.ts`: Миграции схемы БД

#### Декораторы
- `@Required()`: Обязательное поле
- `@Transform({ from: 'isoDate' | 'boolString' | 'dictionary' })`: Правила трансформации
- `@Alias(...)`: Альтернативные имена полей в DTO
- `@Dictionary()`: Поле-словарь
- `@IgnoreOnSave()`: Поле не сохраняется в БД

### UI Layer (UI слой)

#### Composables
- `useEmployees`: Управление сотрудниками (CRUD операции)
- `useDepartments`: Управление отделами
- `useModal`: Управление модальными окнами

#### Components
- `EmployeeList`: Список сотрудников с фильтрацией
- `EmployeeModal`: Модальное окно создания/редактирования
- `DepartmentSelect`: Выбор отдела

## ⚙️ Конфигурация

### Приоритеты источников данных

Файл: `src/config/sources-priority.config.ts`

```typescript
export const SOURCES_PRIORITY: SourcePriority[] = [
  { name: 'sourceA', priority: 10 },
  { name: 'sourceB', priority: 5 },
];
```

Источник с более высоким приоритетом побеждает при конфликтах полей.

### Правила маппинга

Файл: `src/config/mapping.config.ts`

Определяет, какие поля должны преобразовываться в строки `'true'/'false'` при сохранении в API.

## 🔄 Правила трансформации данных

### Boolean строки
- `'true'` → `true`
- `'false'` → `false`
- `true` → `true` (если уже boolean)

### ISO даты
- ISO строка → `Date` объект
- `Date` объект → ISO строка

### Словари
- `{code: string, name: string}` → `DictionaryVO`
- `DictionaryVO` → `{code: string, name: string}`

### Nullable поля
- Пустые строки `''` → `null`
- `null` → `null`

## 🧪 Тестирование

### Unit тесты

Тесты находятся в `src/domain/**/__tests__/`:

- `employee.mapper.test.ts`: Тесты маппера сотрудников
- `employee.service.test.ts`: Тесты сервиса сотрудников

### Запуск тестов

```bash
npm run test
```

## 📝 Примеры использования

### Создание сотрудника

```typescript
import { container } from './infrastructure/di/container';

await container.initialize();
const employeeService = container.getEmployeeService();

const employee = await employeeService.createEmployee({
  firstName: 'Иван',
  lastName: 'Петров',
  email: 'ivan@example.com',
  departmentId: 'dept-001',
  position: { code: 'DEV', name: 'Разработчик' },
  isActive: true,
  isRemote: false,
});
```

### Использование в компоненте

```vue
<script setup lang="ts">
import { useEmployees } from './composables/useEmployees';

const {
  employees,
  loading,
  loadEmployees,
  openCreateModal,
  deactivateEmployee,
} = useEmployees();

onMounted(() => {
  loadEmployees();
});
</script>
```

## 🔧 Расширение

### Добавление новой сущности

1. Создать Entity в `domain/entities/`
2. Создать DTO в `domain/dto/`
3. Создать Mapper в `domain/mappers/`
4. Создать Repository в `domain/repositories/`
5. Создать Service в `domain/services/`
6. Добавить в DI контейнер

### Добавление нового источника данных

1. Создать адаптер в `infrastructure/indexeddb/adapters/`
2. Добавить в `EmployeeRepository` или создать новый репозиторий
3. Обновить приоритеты в `config/sources-priority.config.ts`

### Добавление нового декоратора

1. Создать декоратор в `infrastructure/decorators/`
2. Использовать `defineMeta()` для сохранения метаданных
3. Обновить мапперы для использования метаданных

## 📖 Документация декораторов

### @Required()

Помечает поле как обязательное для валидации.

```typescript
class Employee {
  @Required()
  firstName: string;
}
```

### @Transform()

Указывает правила трансформации при маппинге.

```typescript
class Employee {
  @Transform({ from: 'isoDate' })
  startDate: Date;
  
  @Transform({ from: 'boolString' })
  isActive: boolean;
}
```

### @Alias()

Указывает альтернативные имена полей в DTO.

```typescript
class Employee {
  @Alias('first_name', 'firstName')
  firstName: string;
}
```

### @Dictionary()

Помечает поле как словарь.

```typescript
class Employee {
  @Dictionary()
  position: DictionaryVO;
}
```

## 🐛 Отладка

### Просмотр данных в IndexedDB

1. Откройте DevTools в браузере
2. Перейдите на вкладку Application (Chrome) или Storage (Firefox)
3. Найдите IndexedDB → `vue3-ddd-db`

### Логирование

Мапперы логируют поля, которые были проигнорированы при трансформации (в консоли браузера).

## 📄 Лицензия

MIT

## 👥 Авторы

Создано как архитектурный шаблон для Vue 3 приложений с DDD подходом.
